# 待办与阻塞

更新时间：2026-08-25 20:35（Asia/Shanghai）

当前没有阻塞 I004 完成的待办。个人自用 macOS arm64 DMG/ZIP 与 Windows x64 NSIS EXE/ZIP 已全部落到本地 `release/personal/`，原生安装启动验收和 SHA256 复核均通过。

正式签名、公证、GitHub Release、更新服务器、Intel Mac、Linux 安装包仍是明确延后的非目标，不是当前阻塞项。
