# 待办与阻塞

更新时间：2026-08-25 19:25（Asia/Shanghai）

| 事项 | 负责人 | 优先级 | 下一步 | 解除条件 |
|---|---|---|---|---|
| 个人自用双平台安装包 | Goal 模式执行者 | P0 | 在原生 macOS arm64/Windows x64 环境打包并下载到 `release/personal/` | DMG、macOS ZIP、NSIS EXE、Windows ZIP 均通过安装启动验收且 SHA256 可复核 |

正式签名、公证、GitHub Release 和更新服务器均已明确延后，不是当前阻塞项。Windows 原生 runner 或用户控制的非 `MichengAI` Actions 仓库是 Windows 个人包的必要执行条件；不可用时不得把 I004 标记完成。
