# 当前状态

更新时间：2026-08-25 21:25（Asia/Shanghai）

## 当前阶段

当前工作树版本为 `v1.0.28`，内置 DSH 为 `0.1.1-rc.2`。默认构建、核心启动、验证和桌面更新缺省路径已不依赖 `MichengAI`；运行时采用冻结依赖图、候选验证、原子指针和 last-known-good 回滚。v1.0.27 安装态运行时清单定位缺陷已修复，四个 v1.0.28 个人制品已通过隔离工作目录下的原生安装启动验收并替换本地交付目录。

## 活动迭代

`I004-DSH运行时独立更新` 已完成。v1.0.28 macOS DMG/ZIP 与 Windows NSIS/ZIP 均从无仓库 `package.json` 的工作目录通过原生安装启动验收；有效制品位于 `release/personal/`，失效的 v1.0.27 制品已移入 `release/personal/obsolete-v1.0.27/` 留档。

## 有效风险

- DSH 客户端桥接仍使用隔离的上游 Loader/service adapter；上游契约变化会禁用不支持能力并需要显式适配，不再使用 DOM 文本猜测。
- 当前桌面构建只允许激活自身冻结图覆盖的 DSH 版本；升级到其他版本必须先生成新的 manifest、lock 和桌面构建。
- 本次个人包未签名、未公证且没有正式更新 feed；安装时可能触发 Gatekeeper 或 SmartScreen，只应在 SHA256 匹配后针对本应用单独允许。
- 原 v1.0.27 安装包的冒烟从仓库根目录启动，误读源码 `package.json` 并掩盖了真实安装态缺陷；该制品仅留档，不得继续使用。
