# 当前状态

更新时间：2026-08-25 19:25（Asia/Shanghai）

## 当前阶段

当前工作树版本为 `v1.0.27`，内置 DSH 为 `0.1.1-rc.2`。默认构建、核心启动、验证和桌面更新缺省路径已不依赖 `MichengAI`；运行时采用冻结依赖图、候选验证、原子指针和 last-known-good 回滚。源码闸门与 177 项回归通过。Windows run #3 的构建与 unpacked 启动成功，但 ZIP 首启时冒烟脚本误把候选健康检查的临时监听端口当成最终服务，现改为只接受应用输出的 `DSH_SMOKE_READY` 最终就绪信号；两端需再从新共同提交重建。

## 活动迭代

`I004-DSH运行时独立更新` 正在执行最终原生制品检查点。代码和回归已完成，迭代仍未完成，因为 macOS DMG/ZIP 与 Windows NSIS EXE/ZIP 尚未全部落到本地 `release/personal/` 并通过原生安装启动验收。

## 有效风险

- DSH 客户端桥接仍使用隔离的上游 Loader/service adapter；上游契约变化会禁用不支持能力并需要显式适配，不再使用 DOM 文本猜测。
- 当前桌面构建只允许激活自身冻结图覆盖的 DSH 版本；升级到其他版本必须先生成新的 manifest、lock 和桌面构建。
- macOS arm64 个人包必须在当前 Apple Silicon Mac 完成 DMG/ZIP 安装启动验收；Windows x64 个人包必须由原生 Windows runner 生成并完成 NSIS/ZIP 验收，不能用“等待 CI”关闭 I004。
