# 当前状态

更新时间：2026-08-25 20:35（Asia/Shanghai）

## 当前阶段

当前工作树版本为 `v1.0.27`，内置 DSH 为 `0.1.1-rc.2`。默认构建、核心启动、验证和桌面更新缺省路径已不依赖 `MichengAI`；运行时采用冻结依赖图、候选验证、原子指针和 last-known-good 回滚。源码闸门与 177 项回归通过。共同构建提交 `b197be56c00438dbf45ea6bcd6baed718c667950` 的 macOS arm64 DMG/ZIP 与 Windows x64 NSIS EXE/ZIP 已完成原生验收、哈希复核并交付到本地 `release/personal/`。

## 活动迭代

`I004-DSH运行时独立更新` 已完成。macOS 验收覆盖 DMG 挂载复制与 ZIP 解压启动；Windows run `32847228369` 覆盖 unpacked、ZIP、NSIS 安装后启动、受控退出、卸载、Profile 哨兵保留及 Authenticode `NotSigned` 检查。完整清单见本地 `release/personal/ARTIFACTS.md`。

## 有效风险

- DSH 客户端桥接仍使用隔离的上游 Loader/service adapter；上游契约变化会禁用不支持能力并需要显式适配，不再使用 DOM 文本猜测。
- 当前桌面构建只允许激活自身冻结图覆盖的 DSH 版本；升级到其他版本必须先生成新的 manifest、lock 和桌面构建。
- 本次个人包未签名、未公证且没有正式更新 feed；安装时可能触发 Gatekeeper 或 SmartScreen，只应在 SHA256 匹配后针对本应用单独允许。
