# 当前状态

更新时间：2026-08-25 20:48（Asia/Shanghai）

## 当前阶段

当前工作树版本为 `v1.0.28`，内置 DSH 为 `0.1.1-rc.2`。默认构建、核心启动、验证和桌面更新缺省路径已不依赖 `MichengAI`；运行时采用冻结依赖图、候选验证、原子指针和 last-known-good 回滚。v1.0.27 个人包存在安装后脱离仓库工作目录启动时无法定位 `package.json` 运行时清单的缺陷，旧制品已判定失效，当前正在完成 v1.0.28 双平台热修复验收。

## 活动迭代

`I004-DSH运行时独立更新` 的功能实现已完成，安装态清单定位热修复进行中。完成条件是 v1.0.28 macOS DMG/ZIP 与 Windows NSIS/ZIP 都从无仓库 `package.json` 的工作目录通过原生安装启动验收，并替换本地 `release/personal/` 中的 v1.0.27 失效制品。

## 有效风险

- DSH 客户端桥接仍使用隔离的上游 Loader/service adapter；上游契约变化会禁用不支持能力并需要显式适配，不再使用 DOM 文本猜测。
- 当前桌面构建只允许激活自身冻结图覆盖的 DSH 版本；升级到其他版本必须先生成新的 manifest、lock 和桌面构建。
- 本次个人包未签名、未公证且没有正式更新 feed；安装时可能触发 Gatekeeper 或 SmartScreen，只应在 SHA256 匹配后针对本应用单独允许。
- 原 v1.0.27 安装包的冒烟从仓库根目录启动，误读源码 `package.json` 并掩盖了真实安装态缺陷；该制品不得继续使用。
