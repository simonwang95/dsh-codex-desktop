# 技术架构基线

更新时间：2026-08-25 19:25（Asia/Shanghai）

当前生效的技术硬约束：

- Windows x64、macOS arm64/x64 与 Linux x64，Electron 本地 HTTP 壳。
- 官方 Node `24.19.0`、pnpm `11.22.0`；当前 DSH 运行时锁 `@deepseek-ai/dsh@0.1.1-rc.2`，`@deepseek-ai/dsh*` 使用冻结图并在装配后校验同一目标版本；打包预装；启动不静默升级。
- 官方包不得写入 Web profile；社区插件只写入用户 profile，禁止复制进 DSH 安装目录 `node_modules`。
- 在线安装继续走 `dsh plugin add`；随包 Node 目录必须带 pnpm。
- DSH 使用 `--port 0 --no-open`，仅监听 `127.0.0.1`，不得再打开系统浏览器。
- 仅加载经校验的 loopback HTTP 地址。
- `nodeIntegration: false`、`contextIsolation: true`。
- 正常退出走受控关闭通道；超时仅按 PID 兜底，不全局结束 `node.exe`。
- 卸载不得按安装目录扫杀进程，必须排除卸载器自身。
- 不记录密钥、授权请求头、会话正文或模型输出。
- macOS 签名与公证凭据为后续正式发布配置；当前测试版允许未签名、未公证发布。
- 运行时先装入版本化候选，完成包身份/版本/入口检查和隔离 Profile HTTP 健康检查后再原子切换；首次真实启动失败回滚 last-known-good。
- 默认构建、核心启动和桌面更新缺省配置不得依赖 `MichengAI` 资源；未配置更新 URL 时不得联网检查。
- I004 必须交付当前用户自用的未签名 macOS arm64 DMG/ZIP 与 Windows x64 NSIS EXE/ZIP；四个文件必须来自同一 Git commit，两端均需原生安装启动验收、SHA256 和本地 `release/personal/` 交付清单。Actions 临时 artifact 可以使用，但标签和 GitHub Release 不可使用。

详细唯一事实源：`docs\\03-技术架构\\00-桌面启动器架构基线.md`。
